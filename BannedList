
local BannedUsers = {""}

function isBanned(user)
    for i, v in ipairs(BannedUsers) do
        if v == user then
            return false
        end
    end
    return true
end

if isBanned(game.Players.LocalPlayer.Name) then
    print("your are Banned")
else 
loadstring(game:HttpGet("https://raw.githubusercontent.com/S3nKeTsu/Roblox/main/PizzaPlaceHub"))()
end 


local bannedUsers = {""}

function isBanned(user)
    for i, v in ipairs(bannedUsers) do
        if v == user then
            return true
        end
    end
    return false
end

if isBanned(game.Players.LocalPlayer.Name) then
    print("your are Banned")
else 
loadstring(game:HttpGet("https://raw.githubusercontent.com/S3nKeTsu/Roblox/main/UiLibrary"))()
end 
